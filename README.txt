Packages you will need to install:
libglfw-dev

The program will eventually have separate client and server programs to
coordinate multiplayer support. Currently, only the client has been started.
Each has a (simplistic, possibly temporary) make.sh script.

To shut down the client, hit the "Esc" key.
